# Configuration file for ddclient generated by debconf
#
# /etc/ddclient.conf

protocol=no-ip, \
use=if, if=eth2, server=dynupdate.no-ip.com, login=<%= user %>, password='<%= pass %>', <%= ::hostname %>.no-ip.net
use=if, if=eth0, server=dynupdate.no-ip.com, login=<%= user %>, password='<%= pass %>', <%= ::hostname %>.hopto.me


